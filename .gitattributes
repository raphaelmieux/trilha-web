# Normalise line endings so a Windows checkout does not report every file as
# modified. Text files are stored with LF in the repository and checked out with
# the platform's native ending.
* text=auto eol=lf

# Binary assets must never be line-ending converted, which would corrupt them.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.woff binary
*.woff2 binary
